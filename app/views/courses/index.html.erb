<h2>Course Log</h2>
<table id="students">
  <tr>
    <th>Course</th>
    <th>Logged Hours</th>
    <th>Completed?</th>
    <th>Date</th>
    <% if current_user.admin? %>
      <th>Username</th>
    <% end %>
  </tr>
  <% @courses.each do |course| %>
    <tr>
      <td><%= link_to course.name, course_path(course) %></td>
      <td><%= course.time_spent %></td>
      <td><%= course.completed ? "Yes" : "No" %></td>
      <td><%= course.date %></td>
      <% if current_user.admin? %>
        <td><%= course.student.username %></td>
      <% end %>
    </tr>
  <% end %>
</table>
<ul>
  <%= link_to "Add New Course", new_course_path, class: "submit_button" %>
</ul>
